# One-shot CLI config for AMD — do not replace global config.yaml
default_symbol: AMD
history_period: 3mo
history_interval: 1d

peers:
  - NVDA
  - AVGO
  - INTC

technical:
  ma_windows: [20, 50, 200]
  rsi_period: 14
  macd:
    fast: 12
    slow: 26
    signal: 9

output_dir: output
